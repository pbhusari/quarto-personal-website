# GitLab CI/CD pipeline — builds the Quarto site and deploys to GitHub Pages
#
# REQUIRED: Before this pipeline can run, add a secret variable in GitLab:
#   GitLab project → Settings → CI/CD → Variables
#   Key:   GITHUB_TOKEN
#   Value: a GitHub Personal Access Token with "repo" scope (Contents: Read and Write)
#   Flags: check "Mask variable" so it never appears in logs

stages:
  - deploy

publish:
  stage: deploy
  image: ubuntu:22.04

  variables:
    QUARTO_VERSION: "1.6.39"
    # Prevent git from asking interactive questions
    GIT_TERMINAL_PROMPT: "0"

  before_script:
    # Install the bare minimum needed to download and run Quarto
    - apt-get update -qq
    - apt-get install -y --no-install-recommends wget git ca-certificates
    # Download and install Quarto
    - wget -q "https://github.com/quarto-dev/quarto-cli/releases/download/v${QUARTO_VERSION}/quarto-${QUARTO_VERSION}-linux-amd64.deb"
    - dpkg -i "quarto-${QUARTO_VERSION}-linux-amd64.deb"
    - rm "quarto-${QUARTO_VERSION}-linux-amd64.deb"
    # Verify install
    - quarto --version

  script:
    # Build the site — outputs static HTML to _site/
    - quarto render

    # Deploy _site/ to the gh-pages branch on GitHub
    - git config --global user.email "gitlab-ci@noreply"
    - git config --global user.name "GitLab CI"
    - cd _site
    - git init
    - git checkout -b gh-pages
    - git add .
    - git commit -m "Deploy: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
    # GITHUB_TOKEN is the secret variable you added in GitLab settings
    - git push --force "https://x-token:${GITHUB_TOKEN}@github.com/pbhusari/quarto-personal-website.git" gh-pages

  # Only run this pipeline when changes are pushed to main
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
